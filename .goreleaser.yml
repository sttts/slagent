version: 2

builds:
  - id: slaude
    main: ./cmd/slaude/
    binary: slaude
    goos: [darwin, linux]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w -X main.version={{.Version}}

  - id: slagent-demo
    main: ./cmd/slagent-demo/
    binary: slagent-demo
    goos: [darwin, linux]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w

archives:
  - id: slaude
    builds: [slaude]
    format: tar.gz
    name_template: "slaude_{{ .Os }}_{{ .Arch }}"

  - id: slagent-demo
    builds: [slagent-demo]
    format: tar.gz
    name_template: "slagent-demo_{{ .Os }}_{{ .Arch }}"

brews:
  - name: slaude
    ids: [slaude]
    repository:
      owner: sttts
      name: slagent
    directory: Formula
    homepage: https://github.com/sttts/slagent
    description: Claude Code ↔ Slack bridge
    license: MIT
    install: |
      bin.install "slaude"

release:
  prerelease: auto
